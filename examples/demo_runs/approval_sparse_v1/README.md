This folder contains a curated public demo replay asset for the `main` branch.

It is a tiny schema-v2 Approval run bundled so a fresh clone can launch a replay
without requiring local thesis data, DOE outputs, or hand-in artifacts.

This package is illustrative only. It is not a thesis freeze artifact and not a
benchmark reference bundle.
