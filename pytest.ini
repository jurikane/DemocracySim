[pytest]
markers =
    phase0: Phase 0 reproducibility and replay tests
    phase1: Phase 1 logging & data pipeline tests
    phase2: Phase 2 policy abstraction tests
    phase3: Phase 3 model refactor tests

# Show extra test summary info (xfailed/xpassed) in test output
addopts = -q -r a
